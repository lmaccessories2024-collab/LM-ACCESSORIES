{
  "name": "lm-accessories-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/index.js",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}